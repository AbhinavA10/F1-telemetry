// Package playback will read a series of UDP packet file, stored in .bin format,
// and publish to specified udp port
package playback

import (
	_ "fmt"
	_ "os"
)

// PlayPackets reads bytes from a single supplied path and returns the binary data
func PlayPackets(IP string, PORT int) {

	// filepath := "/media/abhi_ubuntu18/600C7EC53DCD4841/f1/Packets/Packet10.bin"
}
